fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
# PostgreSQL driver — used when DATABASE_URL points at Postgres (e.g. Railway,
# Azure). Not needed for local SQLite, but harmless to install.
psycopg2-binary>=2.9
